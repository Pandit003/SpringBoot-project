spring:
  data:
    mongodb:
      uri: mongodb+srv://JournalApp:Rohit2955@cluster0.xbfvqsk.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: journaldb
      auto-index-creation: true

server:
  port: 9090

logging:
  level:
    com:
      journalproject:
        journalapplication:
          service:
            UserService:
              INFO
            # if you set WARN instead of OFF then WARN log and more priority then warn log is showing

